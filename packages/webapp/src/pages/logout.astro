---
import Layout from '@layouts/Layout.astro';
---

<Layout title="logging out..." hideNav={true}>
  <p>logging out...</p>
  <script>
    const res = await fetch('/api/auth/logout');

    if (res.status === 200) {
      window.location.href = '/login';
    }
  </script>
</Layout>
